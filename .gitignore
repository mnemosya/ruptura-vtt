node_modules/
.env
.env.local
.env.*.local
*.log
.DS_Store
.next/
dist/
build/
*.tsbuildinfo
.claude/

# Sessão local do Playwright para browser checks autenticados (nunca commitar —
# contém cookies de sessão). Ver scripts/dev/save-admin-session.ts.
.auth/

# Material de referência/fonte de design do Console (não servido pelo app —
# os assets usados de fato ficam em public/console/ e public/humanbody/).
/images/
.tmp-validadores/
