# PLANO DE IMPLEMENTAÇÃO — EDITOR UNIVERSAL DA BIBLIOTECA

**Projeto:** Ruptura VTT
**Data:** 14 de julho de 2026
**Status:** Plano técnico pós-auditoria (Etapa 0). Nenhuma etapa de construção autorizada por este documento — cada etapa segue exigindo checkpoint próprio, conforme `docs/ADITIVO_PRD_EDITOR_UNIVERSAL_CONTEUDO.md` §17/§22.
**Depende de:** `docs/AUDITORIA_EDITOR_UNIVERSAL_CONTEUDO.md`, `docs/SCHEMA_CANONICO_CONTEUDO_V1.md`.

---

## 1. Recorte exato do MVP

Confirmando e detalhando as decisões já consolidadas no aditivo §24, ajustadas aos achados reais da auditoria:

**Tipos de conteúdo no MVP:** magia, talento, item/equipamento (exatamente os três do aditivo §Etapa 3 — são os três com maior volume real: 132, 22/66, 120 registros, e os únicos com `estatisticas`/`efeitos` já ricos e testados por scripts existentes).

**Efeitos no MVP:** dano, cura, aplicar condição, remover condição, modificar teste, alterar recurso (exatamente os 6 do aditivo §Etapa 4). Da auditoria:
- `cura` e `remover_condicao` já têm executor real — são o caminho mais seguro para provar o fluend-to-end do editor sem esperar novo trabalho de engine.
- `dano` tem executor parcial (rola, nem sempre aplica) — o MVP do editor pode gerar o payload canônico mesmo que a aplicação em jogo ainda dependa de trabalho de engine already-scoped.
- `aplicar_condicao` **não tem executor hoje** apesar de ser o efeito mais comum do conteúdo — o MVP do editor deve deixar claro (diagnóstico técnico) que esse efeito nasce como "lembrete" até a Etapa 4 conectar um executor genérico.
- `modificar_teste`/`alterar_recurso` têm executor parcial e fragmentado em 3 módulos (`activeEffects.ts`, `talents.ts`, `technicalEffects.ts`) — o MVP do editor não precisa esperar a consolidação desses módulos, só precisa gerar o payload no formato que os três já entendem hoje (`valor` + `alvo_tags`).

**Fora do MVP** (explicitamente, para não gerar expectativa): condição, runa, escalpo, propriedade, ação como tipos editáveis; efeitos compostos (teste/resistência com ramificação), efeitos temporários, consumo, drone/robô/trama, mercado avançado, homebrew de mesa, importação/exportação completa, grupos lógicos E/OU de condição, editor de fórmula livre.

**Perfil de usuário no MVP:** só Administrador da Biblioteca Oficial (aditivo §6.2 já permite restringir o MVP a isso). Como hoje **não existe nenhuma camada de admin/permissão** (achado da auditoria §9), a Etapa 2 precisa necessariamente incluir a criação dessa camada — não é opcional, é pré-requisito de segurança antes de qualquer escrita.

---

## 2. Plano incremental (mapeado às etapas já definidas no aditivo)

O aditivo já define 13 etapas (0 a 12) com objetivo/critério de aceite próprios. Este plano não as reescreve — só anota, por etapa, o que a auditoria revelou que **muda o esforço esperado** em relação ao texto genérico do aditivo.

### Etapa 1 — Camada canônica de definições e validação — **CONCLUÍDA**
- Trabalho adicional revelado pela auditoria: como não existe hoje nenhuma taxonomia de efeito compartilhada (150 valores de `tipo` distintos, cada content_type com vocabulário próprio), a Etapa 1 precisa necessariamente incluir a criação do catálogo de efeitos (`docs/SCHEMA_CANONICO_CONTEUDO_V1.md` §3) como artefato de primeira classe, não como detalhe de implementação.
- Adaptadores de leitura precisam ser **um por content_type**, não genéricos — confirmado pela heterogeneidade real dos payloads (duração em 4 formatos, resistência em 2 formatos).
- Implementado em `src/lib/contentSchema/` — registro de tipos de conteúdo, catálogo de efeitos, normalizadores de duração/resistência/referência, validação, diagnóstico de automação, adapters para spell/talent/item/condition + fallback genérico para os 8 content_types restantes, e os 5 exemplos canônicos obrigatórios. Detalhes completos em `docs/CHECKPOINT_ETAPA1_SCHEMA_CANONICO.md`. Ajustes em relação a esta proposta estão documentados em `docs/SCHEMA_CANONICO_CONTEUDO_V1.md` §8.

### Etapa 2 — Lista administrativa e inspeção — **CONCLUÍDA**
- Pré-requisito de segurança descoberto na auditoria: **não existe hoje nenhum admin role**. Esta etapa precisa entregar, além da lista read-only, a própria noção de "quem é admin" (tabela/coluna de role, ou allowlist de e-mails autenticados via Supabase Auth) — sem isso, "acesso indevido é bloqueado" (critério de aceite do aditivo) não tem como ser verdadeiro.
- Diagnóstico técnico já tem um caso real para mostrar: a divergência de contagem de itens (120 real vs. 119 no manifesto) — bom teste de aceite orgânico, não fabricado.
- Implementado: migration `0020_content_admin_roles.sql` (tabela `admin_users` + função `is_content_admin()` SECURITY DEFINER, sem policy nova em `content_documents`), rota `/admin/biblioteca` (lista) e `/admin/biblioteca/[contentType]/[slug]` (detalhe), gate único em `src/app/admin/layout.tsx`. Detalhes completos, incluindo browser check confirmado, em `docs/CHECKPOINT_ETAPA2_ADMIN_BIBLIOTECA_READONLY.md`.

### Etapa 3 — Editor universal de campos básicos — **CONCLUÍDA**
- Já compatível com o recorte real: magia/talento/item são os três com `identificacao`/`classificacao`/`texto` mais uniformes (ver matriz §2 do schema canônico).
- Atenção: `estatisticas` de item é campo livre no schema atual — a Etapa 3 deve tratá-lo como somente leitura/técnico neste momento (não gerar formulário para um campo sem contrato), conforme decisão em aberto §7.3 do schema canônico.
- Implementado: rascunhos em tabela separada `content_drafts` (migration `0021_content_drafts.sql`, RLS admin-only via `is_content_admin()`), editor universal em `/admin/biblioteca/rascunhos/*`, Server Actions em `src/lib/contentSchema/draftServerActions.ts` (criar/editar/duplicar/atualizar/excluir), controle de concorrência otimista (coluna `version`). Detalhes completos e browser check confirmado em `docs/CHECKPOINT_ETAPA3_EDITOR_CAMPOS_BASICOS.md`. Ajuste incidental: corrigido um bug latente da Etapa 1 (spell adapter não copiava `pericia_teste`/`atributo_ataque` para `classificacao`).

### Etapa 4 — Construtor de efeitos MVP — **Etapa 4 concluída — aceite operacional e de browser aprovado (ver checkpoint §9.3)**
- `cura` e `remover_condicao` — caminho mais seguro para o primeiro efeito ponta a ponta (executor já existe).
- `aplicar_condicao` — maior valor de produto (é o efeito mais comum), mas exige construir o primeiro executor genérico novo (hoje não existe nenhum). Recomendação: tratar como o efeito "prova de conceito" desta etapa, já que o aditivo pede explicitamente que a magia `1d8` de fogo funcione fim a fim (critério §21) e ela tipicamente usa `aplicar_condicao` em resistência falha.
- `dano`/`modificar_teste`/`alterar_recurso` — gerar payload canônico compatível com os executores parciais já existentes, sem exigir refatorar `activeEffects.ts`/`talents.ts`/`technicalEffects.ts` nesta etapa (risco de regressão desnecessário fora de escopo).
- Implementado: `src/lib/contentSchema/effectDraft*.ts` (tipos/mapeamento/diagnóstico/validação compartilhados pelos 6 efeitos), UI em `rascunhos/_shared/EffectsEditorSection.tsx` + `EfeitoCamposPorTipo.tsx`, executor genérico `src/lib/character/conditionEffectExecutor.ts` conectado ao fluxo manual de `/dev/table`. Catálogo de `aplicar_condicao` (Etapa 1) atualizado de "lembrete" para "assistido". Detalhes completos em `docs/CHECKPOINT_ETAPA4_CONSTRUTOR_EFEITOS_MVP.md`.
- **Rodada de conclusão do aceite operacional (25/07/2026)**: os únicos itens do checklist original de 25 que nunca tinham sido concluídos em execução alguma — 21/22/23, o consumo operacional real do executor genérico de `aplicar_condicao` via `/dev/table` (aplicar condição real em personagem real, persistência após reload, segunda tentativa bloqueada sem duplicar) — foram executados e confirmados ao vivo contra o Supabase real, com fixtures próprias (mesa/personagem/perfil dedicados, nunca reaproveitando os fixtures compartilhados de rodadas anteriores) removidas ao final. Nenhum bug foi encontrado (o executor funcionou exatamente como já documentado desde a implementação original desta etapa) — achado de ambiente registrado sem alteração de código: `/dev/table` hoje exige narrador autenticado (as policies de RLS de `characters` não têm mais leitura anônima aberta), tornando desatualizado o comentário "sem autenticação" no código — não corrigido por ser dívida cosmética fora do escopo. Etapa 4 promovida de "aceite operacional parcial" para **"Etapa 4 concluída — aceite operacional e de browser aprovado"**. Nenhuma outra etapa foi tocada ou promovida. Detalhes completos em `docs/CHECKPOINT_ETAPA4_CONSTRUTOR_EFEITOS_MVP.md` §9.3.

### Etapa 5 — Publicação, versões e changelog — **Etapa 5 concluída — publicação, versões, changelog e concorrência aprovados (ver checkpoint §"Concorrência otimista real")**
- `content_changelog` já existia mas **não** resolvia sozinho versões/comparação — foi **estendido** (versões, autor, resumo, caminhos, impacto, hashes, vínculo com rascunho), não recriado. Publicação/arquivamento passam por funções SECURITY DEFINER transacionais (`publish_content_draft`/`archive_content_document`, migration `0022_content_publishing.sql`); a versão (`+patch`, `1.0.0` para novo) é autoridade do SQL, sincronizando coluna `version`, `payload.versao` e changelog.
- Rascunho isolado em `content_drafts` (Etapa 3) permanece a fonte de rascunho — publicar consome o rascunho e escreve `content_documents` (published); arquivar usa `status='archived'`, que some do jogo por construção (todas as queries de consumo já filtram `published`). Detalhes, estratégia de serialização, segurança e limitações em `docs/CHECKPOINT_ETAPA5_PUBLICACAO_VERSIONAMENTO.md`.
- **Verificado**: TypeScript, build e o núcleo transacional (por SQL direto, com rollback e zero resíduo). **Pendente**: browser check (bloqueado pelo esbuild — não executado, não alegado como aprovado).
- **Correção de compatibilidade** (pós-aceite, mesma etapa): a serialização inicial embutia um blob `_editor` dentro de cada efeito publicado — inválido contra `additionalProperties: false` dos schemas oficiais de magia e talento. Corrigido: metadata do editor movida para tabela administrativa separada (`content_editor_metadata`), serialização reescrita para emitir só chaves confirmadas pelos schemas reais, validação bloqueante para configurações não representáveis no vocabulário legado, e round-trip completo preservado. Nenhum conteúdo real chegou a ser contaminado. Detalhes completos em `docs/CHECKPOINT_CORRECAO_METADATA_EDITOR_PUBLICACAO.md`.
- **Correção crítica** (auditoria formal do Editor Universal, rodada posterior): `existeSlugColidindo` (`draftValidation.ts`) tratava qualquer documento publicado com o mesmo `content_type`+slug como colisão, mesmo quando era o próprio documento-base do rascunho de edição — bloqueando "Salvar rascunho" em TODA edição de conteúdo já publicado, para os 5 tipos editáveis, e também rascunhos de importação `atualizacao`. Corrigido passando `base_document_id` até `existeSlugColidindo`, que agora só considera colisão um documento publicado com `id` diferente do documento-base. Validado por harness puro (46/46, reproduzindo o defeito antes da correção) e por fluxo real de browser contra o Supabase real (capítulo e item: criar→publicar→editar→salvar→revisar→republicar→persistir, sem falso bloqueio). Etapa 5 reclassificada de "Parcial" para "Implementada — aceite de browser pendente" (o fluxo de edição/publicação em si foi validado; o aceite de browser completo dos 25 itens originais continua pendente). Detalhes completos em `docs/CHECKPOINT_CORRECAO_COLISAO_SLUG_PUBLICACAO.md`.
- **Correção** (rodada posterior, mesma auditoria formal): `serializarItem` (`publishSerialization.ts`) nunca emitia `categoria_label`/`raridade_label`, exigidos pelo schema oficial de equipamentos — o round-trip "exportar item → reimportar sem edição manual do JSON" falhava com "Inválido contra o schema oficial" para qualquer item publicado pelo editor (achado registrado na rodada anterior, ver nota acima). Corrigido: labels derivados no servidor a partir de `categoria`/`raridade` (nunca aceitos do client), em módulo canônico único (`itemLabels.ts`); checagem bloqueante nova na revisão de publicação, reaproveitando o mesmo validador de schema oficial já usado pela importação. Provado ao vivo, sem edição manual: exportar → reimportar → "Idêntico ao publicado"; exportar → alterar campo com a função de hash real → reimportar → "Atualização" → salvar/revisar/republicar (`1.0.1`). Dois harnesses de regressão novos (`validate-item-schema-roundtrip.mjs` 12/12, `validate-post-mvp-effects.mjs` 17/17 — este último cobrindo pela primeira vez com teste automatizado a correção `e584abb` de efeitos pós-MVP). Detalhes completos em `docs/CHECKPOINT_CORRECAO_SCHEMA_ITEM_ROUNDTRIP.md`.

### Etapa 6 — Adaptadores e edição de legados — **Etapa 6 concluída — adaptadores e edição de legados aprovados (ver checkpoint §"Rodada de conclusão")**
- A classificação de 5 estados (`conversao_direta`/`conversao_com_confirmacao`/`somente_leitura`/`incompativel`/`invalido`) já existia desde a Etapa 1, mas por DOCUMENTO inteiro — a Etapa 6 refinou para por CAMPO/EFEITO (`legacyConversion.ts`), reaproveitando os adapters e normalizadores existentes sem duplicar parsing.
- Fluxo de conversão explícito: diagnóstico (`/admin/biblioteca/rascunhos/legado/[contentType]/[slug]`) → confirmação de campos ambíguos → rascunho com `origemLegado` (adapter+versão, decisões, campos/efeitos preservados) → publicação com validação de perda (`legacyLossValidation.ts`) → changelog registra a origem da conversão. Nenhuma migration nova (reusa JSONB de `content_drafts`/`content_editor_metadata`/`content_changelog.impact`, todos já existentes).
- **Verificado**: TypeScript, build, classificação/relatório e validação de perda com cópias de registros reais (spell/talent/item), núcleo transacional por SQL direto com rollback. Detalhes completos em `docs/CHECKPOINT_ETAPA6_ADAPTADORES_LEGADO.md`.
- **Rodada de conclusão (25/07/2026)**: fechado o ciclo completo salvar→revisar→publicar→republicar sobre conteúdo genuinamente legado, nunca antes exercitado ao vivo (a rodada original havia decidido não tocar conteúdo oficial real além do diagnóstico). Usada uma cópia temporária de `item:faca` (inserida diretamente via SQL, sem `content_editor_metadata` — genuinamente legada, isolada do conteúdo real): campo conhecido alterado e persistido, publicado (1.0.0→1.0.1), reeditado uma segunda vez (1.0.1→1.0.2) sem acúmulo de metadata, campos desconhecidos (objeto aninhado/array/número/string/boolean) preservados byte a byte. Fallback genérico (`combat_action:deslocar`) e tipo somente leitura (`condition:atordoado`, incl. 404 real do servidor na rota de conversão) confirmados coerentes. Smoke check de spell/talent confirma os 3 adapters editáveis reconhecendo conteúdo real. Detalhes completos em `docs/CHECKPOINT_ETAPA6_ADAPTADORES_LEGADO.md` §"Rodada de conclusão".

### Etapa 7 — Teste, resistência e efeitos compostos — **Etapa 7 concluída — testes, resistências e efeitos compostos aprovados (ver checkpoint §"Rodada de conclusão")**
- Novo tipo canônico `teste_resistencia` (raiz, com resultados/faixas reusando o enum real `MARGEM_CLASSIFICACOES` de `src/lib/dice/types.ts`) + `modificar_margem` (serializa exatamente o formato real já lido por `getMarginPromotions`/`talentEngine.ts`) + `alterar_dano_recebido` (novo, sempre `lembrete` — sem executor real no motor hoje). Profundidade limitada por CONSTRUÇÃO (o tipo `EfeitoFilho` exclui `teste_resistencia` — sem recursão possível, não só validada em runtime).
- Serialização legada auditada schema por schema: magia/item representam a árvore como o efeito-raiz de resistência + até 2 efeitos-filho *siblings* (mesma convenção real já usada em conteúdo publicado); além disso, ou talento, ou CD derivada em item, ficam bloqueados com mensagem clara — nunca "fingindo suporte". `efeito_com_resistencia` legado nunca é auto-convertido (3 estruturas reais incompatíveis encontradas entre spell/rune/property).
- **Verificado**: TypeScript, build, classificação/serialização contra os 3 schemas oficiais (14 casos reais), núcleo transacional por SQL com rollback. Corrigido de passagem um bug real em `publishDiff.ts` (comparava por um `_editor.id` que não existe mais desde a correção pós-Etapa 5). Detalhes completos em `docs/CHECKPOINT_ETAPA7_EFEITOS_COMPOSTOS.md`.
- **Rodada de conclusão (25/07/2026)**: fechada a reedição de conteúdo publicado com `teste_resistencia` — nunca exercitada em rodada alguma até agora. **Achado crítico ao vivo**: reeditar e republicar uma árvore composta (recuperada de `content_editor_metadata`) DUPLICAVA o `efeito_com_resistencia` a cada ciclo (confirmado real no Supabase — 1→2→3 cópias) — mesma classe de bug do `e584abb`, só que em `publishSerialization.ts::ehEfeitoMvpLegado`, que checava só os 6 tipos originais do MVP em vez de considerar os tipos das Etapas 7–10. Corrigido de forma sensível à origem do rascunho: conversão de legado (`origemLegado` presente) mantém a regra antiga (nunca auto-converte — preserva a coexistência documentada em §5); edição normal/reedição (`origemLegado` ausente) agora substitui corretamente, nunca duplica. Regressão provada (harness `validate-composite-effects.mjs`, caso novo #15, falha contra o código anterior, passa depois; caso #5 preexistente ajustado e continua passando). Validações negativas confirmadas bloqueando de verdade na tela de revisão (não só no harness); efeitos filhos de 2 tipos distintos (`dano`+`aplicar_condicao`) na mesma árvore confirmados. Consumo operacional continua fora do critério de aceite (decisão formal preexistente, reconfirmada — resolvedor real sem gatilho de UI). Detalhes completos em `docs/CHECKPOINT_ETAPA7_EFEITOS_COMPOSTOS.md` §"Rodada de conclusão".

### Etapa 8 — Efeitos temporários, cadências, usos e consumos — **Etapa 8 concluída — efeitos temporários, cadências, reaplicação e consumos aprovados (ver checkpoint §"Correção da divergência de reaplicação/pilhas")**
- Novo tipo canônico `efeito_temporario` (duração/pilhas/política de reaplicação no mesmo vocabulário de `TemporaryEffect`, `character/types.ts`; modificadores filhos reaproveitam `modificar_teste`, limitados a 4, sem recursão) + `acao_reacao_adicional` (novo, sempre `lembrete` — nenhum executor real concede ação/reação/ataque adicional hoje). Uso/cadência NÃO é um tipo novo: campo `usoLimitado` reutilizável em qualquer efeito, serializando para o mesmo `usos`/`cadencia` real já lido por `talentEngine.ts`. Consumo de recurso (PA/Reação/Mana/Sobrecarga/RAM) reaproveita `alterar_recurso` (Etapa 4) com 2 campos novos (`momentoConsumo`/`refundEmCancelamento`) — nunca um tipo/executor duplicado. Consumo de carga/munição/flecha da Aljava NÃO ganhou tipo de efeito — já totalmente representado por `consumeItemCharge`/`consumeAttackAmmo` (reais, genéricos, fora do catálogo de efeitos).
- Serialização auditada schema por schema: item serializa `efeito_temporario` como `buff_temporario` real (mesmo shape já lido por `buildTemporaryEffectFromStructuredPayload`); talento serializa uso/cadência e ação adicional nos campos/família reais (`usos`/`cadencia`, `familia: "ataque_adicional"|"reacao"`); magia bloqueia os 3 tipos (schema fechado, sem campo de duração/buff/uso/cadência).
- **Verificado**: TypeScript, build, serialização/validação contra os 3 schemas oficiais (17 casos reais, incluindo round-trip pelo EXECUTOR REAL de efeito temporário e expiração/acúmulo de pilhas real), núcleo transacional por SQL com rollback. **Pendente**: browser check (esbuild) e um gatilho de UI ao vivo (reaproveita o fluxo de uso de item já existente, sem novo botão). Detalhes completos em `docs/CHECKPOINT_ETAPA8_TEMPORARIOS_CADENCIAS_CONSUMOS.md`.

### Etapa 9 — Inventário, equipamentos, runas e mercado — **Etapa 9 concluída — inventário, runas e mercado aprovados (ver checkpoint §"Rodada de conclusão")**
- `rune` promovida a 4º content type editável do Editor Universal (já existia no enum `content_type` desde a migration 0001, e `contentTypeRegistry.ts` já sinalizava "planejado ... até Etapa 9"). Reaproveita 100% do pipeline existente (mesmo catálogo de efeitos, `content_editor_metadata`, RPC de publicação, diff/histórico).
- `CamposItem` ganhou defaults de modelo REAIS (MIT-base/PD-base/slots de runa/cargas máximas/munição máxima/munição compatível) escrevendo nas mesmas chaves `estatisticas.*` já lidas por `normalizeItemContent` — verificado round-trip completo pelo leitor real.
- Novo tipo `modificar_instancia` (operação enumerada, nunca caminho JSON livre) conecta reparo de MIT/PD a executores REAIS já existentes (`setItemMitAtual`/`setItemPdAtual`) e resolve a lacuna que `docs/RELATORIO_AUTOMACAO_RUNAS.md` apontava para as runas de autorreparo ("nenhum modelo de recurso MIT/PD existe" — agora existe). Instalação/remoção/ativação de runa já eram reais e genéricas (`installRuneOnItem`/`removeRuneFromItem`/`toggleInstalledRune`) — nenhum código novo foi necessário para esse fluxo, só documentado.
- `alterar_preco` serializa exatamente nos 2 formatos REAIS já lidos genericamente por `getGarimpoDeRuaAvailability`/`getCadernetaDeDividaAvailability` (desconto de loja e compra fiada/dívida) — as demais operações de preço, `conceder_item`/`consumir_item`/`alterar_disponibilidade`, não têm executor real conectado (sempre lembrete, por decisão deliberada de escopo — "não transformar esta etapa em editor geral de inventário").
- **Verificado**: TypeScript, build, serialização/validação contra os 4 schemas oficiais (22 casos reais, incluindo round-trip pelo `normalizeItemContent`/`setItemMitAtual`/`getRuneCompatibility`/`hasExistingAljava` REAIS), núcleo transacional por SQL com rollback. **Pendente**: browser check (esbuild). Detalhes completos em `docs/CHECKPOINT_ETAPA9_INVENTARIO_RUNAS_MERCADO.md`.

### Etapa 10 — Drones, robôs, companheiros e Trama — **Etapa 10 concluída — companheiros, drones, robôs e Trama aprovados (ver checkpoint §"Rodada de conclusão")**
- Auditoria confirmou dois sistemas separados: Droneiro/Mecatrônico/Tecelão já são ~15 funções bespoke reais em `talentEngine.ts` (registerDrone/iniciarTrama/executarBypass/etc.), com CRUD completo em `Character.drones[]`/`robos[]`/`trama_ativa` e ciclo de vida ligado a fim de rodada/cena — mas nenhuma é genérica, e o Editor Universal nunca conseguia editá-las (`legacyConversion.ts::FAMILIAS_INCOMPATIVEIS` já marcava "companheiro"/"trama" como incompatível desde antes desta etapa).
- Decisão: nenhum `content_type` novo (drone/robô não são entidades reutilizáveis — `modelo`/`acoes`/`ordens` são texto livre até na instância real, sem catálogo publicado). 6 novos tipos no catálogo universal existente (`companheiro`, `modificar_companheiro`, `acao_companheiro`, `programar_gatilho`, `parear`, `acao_trama`), representáveis só em talento (`familia:"companheiro"`/`"trama"`, valores reais do enum), sempre `lembrete` (nenhum executor genérico existe). RAM reaproveita `alterar_recurso` (`recurso:"ram"`, já suportado, zero código novo).
- **Achado importante**: ao validar contra o schema real de talento (fechado, ~90 chaves fixas), descobri e corrigi 5 bugs reais de etapas anteriores (Etapa 8/9) que emitiam chaves inexistentes nesse enum para talento (`efeito_temporario`, `modificar_instancia`, `conceder_item`, `consumir_item`, `alterar_disponibilidade`) — nunca detectados porque os harnesses anteriores só validaram esses tipos contra item/rune (schema aberto, mascarava o problema).
- **Verificado**: TypeScript, build, serialização/bloqueio dos 6 tipos novos + as 5 correções, validados contra `schema_talentos_v1_3.json` real (23 casos), núcleo transacional por SQL com rollback. **Pendente**: browser check (esbuild). Detalhes completos em `docs/CHECKPOINT_ETAPA10_COMPANHEIROS_TRAMA.md`.
- **Rodada de aceite de browser das Etapas 4–10 (auditoria formal do Editor Universal, sessão posterior)**: confirmado que o bloqueio de `esbuild`/`tsx` citado em todas as etapas acima continua genuinamente presente neste ambiente (`@esbuild/darwin-x64` instalado, `arm64` necessário) — os scripts `check-admin-*.ts` seguem não executáveis sem reinstalar dependências (fora de escopo). Em vez disso, a mesma ferramenta de browser já usada nas Etapas 11/12 foi usada para exercitar ao vivo, contra o Supabase real, os fluxos editoriais centrais de cada etapa (criar/configurar/salvar/persistir/publicar cada tipo de efeito novo). **Achado crítico**: `effectDraftValidation.ts` ainda usava `isTipoEfeitoMvp` (só os 6 tipos originais) como porta de entrada da validação de rascunho, em vez de `isTipoEfeitoEditavel` (os 22 tipos reais) — bloqueava "Salvar rascunho" para QUALQUER conteúdo usando `teste_resistencia`, `efeito_temporario`, ou qualquer um dos 11 tipos das Etapas 9/10, desde que o primeiro deles foi introduzido na Etapa 7. Nenhum harness puro detectou isso (todos testam a camada de serialização de publicação, não a de validação de rascunho). Corrigido (troca de 2 linhas). Após a correção, confirmados ao vivo e publicados com sucesso, com payload verificado diretamente no Supabase real: `teste_resistencia` (spell, 2 resultados + efeito filho), `efeito_temporario` (item, com modificador filho — automação "Assistido" como já documentado), e os 7 tipos das Etapas 9/10 juntos num único talento (`modificar_instancia`/`conceder_item`/`consumir_item`/`alterar_disponibilidade`/`companheiro`/`acao_trama`), confirmando pela primeira vez ao vivo a correção de serialização que a Etapa 10 já havia aplicado (§5) mas nunca verificado fora do harness. Etapa 5 também recebeu o ciclo completo editar/salvar/revisar/republicar e o teste obrigatório de importação classificada como `atualizacao` (end-to-end via a UI real de exportar/importar, incluindo hash canônico recomputado com a função real). Etapa 6 recebeu diagnóstico + criação de rascunho de edição sobre conteúdo legado real (`item:faca`, nunca alterado). Nenhuma etapa foi promovida a "concluída — aceite de browser aprovado" (checklists originais completos não foram reexecutados item a item; vários itens de cada etapa não têm UI dedicada, cobertos só pela suíte node) — todas passam a "aceite de browser parcial", um avanço real de "pendente" (nenhuma evidência de browser) para evidência real, incluindo a correção de um bug que bloqueava totalmente o uso desses recursos. Detalhes completos em `docs/CHECKPOINT_CORRECAO_VALIDACAO_TIPOS_EFEITO_POS_MVP.md` e nas seções "Aceite de browser" adicionadas a cada checkpoint de Etapa 4–10.
- **Rodada de conclusão das Etapas 8–10 (25/07/2026)**: fechadas as lacunas residuais que ainda restavam em cada checkpoint — aplicação operacional real de `efeito_temporario` (instância criada/reaplicada/expirada num personagem de fixture real, via `/dev/character-sheet` e `/dev/table`), instalação de runa e reparo de MIT via executor real numa instância real (Etapa 9), e reedição dos 6 tipos novos da Etapa 10. **Achado crítico**: reeditar/republicar qualquer conteúdo com `companheiro`/`modificar_companheiro`/`acao_companheiro`/`programar_gatilho`/`parear`/`acao_trama`/`efeito_temporario`/`modificar_instancia`/`conceder_item`/`consumir_item`/`alterar_disponibilidade`/`alterar_dano_recebido` em TALENTO duplicava a entrada a cada ciclo de edição→publicação, e um tipo removido no editor podia reaparecer no payload publicado — causa raiz em `effectTypeRegistry.ts` (`aliasesLegado.talent` de 9 tipos não incluía o próprio `tipo` legado que cada um produz, então `resolverTipoCanonico` nunca reconhecia a entrada antiga como substituível). Corrigido adicionando os aliases que faltavam (só dados, nenhuma lógica nova); reproduzido rigorosamente contra o código anterior (falha confirmada, depois corrigida) em dois harnesses (`validate-composite-effects.mjs` caso #15, `validate-companions-trama.mjs` caso #24, novos). Também corrigido, de passagem: o select de raridade de item já tinha sido corrigido numa rodada anterior (`f896b43`) — nesta rodada, achado adicional registrado (não corrigido, por exigir mudança de regra de jogo): a política de reaplicação/pilhas de `efeito_temporario` configurável no editor para item não é lida pelo executor real (`stackingMode` sempre "replace"). Detalhes completos em `docs/CHECKPOINT_ETAPA8_TEMPORARIOS_CADENCIAS_CONSUMOS.md`, `docs/CHECKPOINT_ETAPA9_INVENTARIO_RUNAS_MERCADO.md` e `docs/CHECKPOINT_ETAPA10_COMPANHEIROS_TRAMA.md`, seção "Rodada de conclusão do aceite" em cada um.

### Etapa 11 — Importação, exportação e Biblioteca do Livro — **Etapa 11 concluída — integração editorial de drag aprovada**
- Auditoria original confirmou: `content_packs` é registro de linhagem, nunca reaproveitado como contrato de arquivo; nenhum sistema de capítulo/Livro existia em runtime; nenhum código de drag-and-drop existia em lugar nenhum do projeto.
- Contrato novo e isolado `ruptura-content-package` v1 (`contentPackage.ts`), hash canônico próprio (`canonicalHash.ts`), exportação unitária/em lote admin-only (`packageExport.ts`), importação draft-only com as 11 classificações exigidas + validação de schema oficial (`officialSchemaValidator.ts`) + dependências estruturadas (`contentDependencies.ts`) + preview recalculado 100% no servidor (`importPreview.ts`, `packageImport.ts`); histórico de importações (`content_import_sessions`) e vínculos editoriais Biblioteca↔Livro (`content_book_links`).
- Migration 0024: `content_import_sessions`, `content_book_links`, RPC transacional `import_content_drafts`.
- **Correção do drag editorial (esta sessão)**: a lacuna estrutural que bloqueava a etapa — nenhum content_type representava um capítulo do Livro, logo não havia destino real para um drop — foi fechada. Migration nova `0035_add_capitulo_content_type.sql` (aplicada ao Supabase real) adiciona "capitulo" ao enum `content_type`; nenhum outro objeto mudou (a tabela/RPC de conteúdo já eram genéricas por tipo). "Capítulo" passa a ser um 5º content_type editável no MESMO Editor Universal (nenhum segundo editor criado): payload com `blocos` ordenados (texto livre ou referência estruturada `{contentType, slug}` a conteúdo real da Biblioteca — nunca o payload completo). Drag nativo HTML5 (sem nova dependência) entre um painel de busca embutido no editor e a lista de blocos do capítulo, sempre com um botão "+ adicionar"/"▲"/"▼" equivalente sem mouse. Validação real no servidor (`validarCamposCapitulo`): existência da entidade referenciada, bloqueio de auto-referência e de duplicidade — nunca confia no client. Persistência pelo pipeline de rascunho/publicação já existente (mesmo optimistic locking, mesma RPC transacional de publicação).
- **Aceite de browser executado por completo**: criação, edição, drag/alternativa por botão, reordenação, salvar, reload (persistência confirmada), publicação, exportação, reimportação (classificada "Idêntico ao publicado" — round-trip sem perda comprovado por hash canônico), edição de conteúdo já publicado, e os 2 casos hostis pedidos (auto-referência e duplicidade), ambos corretamente rejeitados com erro coerente, sem crash. Console e rede limpos em todas as etapas.
- **3 bugs reais encontrados e corrigidos durante o aceite** (nenhum aparecia nos harnesses puros): `contentPackage.ts::ehContentTypeEditavel` e `packageImport.ts::TIPOS_VALIDOS` duplicavam a lista dos 4 tipos editáveis por nome em vez de reaproveitar `isDraftContentType` — divergiram silenciosamente ao adicionar "capitulo", rejeitando a importação de um capítulo real; `draftBuilders.ts::montarCamposECamposDesconhecidosIniciais` não tinha branch para "capitulo" — "Criar rascunho de edição" a partir de um capítulo publicado produzia um rascunho com tipo internamente inconsistente, quebrando o render. Todos corrigidos minimamente, sem editar nenhuma migration aplicada.
- **Verificado**: TypeScript, build, harness da Etapa 11 (`validate-import-export-book.mjs`) 19/19 + novo harness da correção (`validate-editorial-drag.mjs`) 6/6, aceite de browser completo, round-trip export/import contra Supabase real, Etapa 12 revalidada sem regressão (18/18 + 78/78). Detalhes completos em `docs/CHECKPOINT_ETAPA11_IMPORTACAO_EXPORTACAO_BIBLIOTECA_LIVRO.md` (`## 0-9`).
- **Validação final do drag nativo e da Biblioteca (rodada seguinte, sem alterar código)**: uma auditoria posterior encontrou duas lacunas de comprovação genuínas no aceite anterior — (1) não estava documentado se o painel de busca embutido em `CamposCapituloSection` era uma superfície válida da Biblioteca; (2) só o botão "+ adicionar"/▲/▼ tinha sido exercitado, nunca um gesto real de `dragstart`→`dragover`→`drop`. Auditoria documental (PRD §2.1.9, aditivo §11.11/§18.3) confirmou que o PRD descreve um drag DIFERENTE (Livro→ficha, operacional, fora de escopo) e que o aditivo (§18.3: "referências grandes com pesquisa; não carregar a Biblioteca inteira em cada modal") antecipa exatamente o padrão de painel de busca escopado implementado — confirmado que ele é uma superfície editorial legítima (mesma fonte de dados/resolvedor/RLS que `/admin/biblioteca`, redução deliberada de campos, nenhuma duplicação divergente). O gesto real foi então executado despachando eventos DOM reais (`dragstart`/`dragenter`/`dragover`/`dragleave`/`drop`/`dragend`) com um `DataTransfer` real (nunca chamando a função de domínio diretamente) — confirmando MIME/payload, feedback visual real (mudança de cor no `dragover`, reversão no `dragleave`), inserção, persistência após reload, e todos os casos inválidos (duplicidade, auto-referência, item inexistente, payload malformado, MIME ausente, drop fora da área) através do próprio caminho de drag, convergindo para a mesma validação de domínio do botão. Um achado correlato pré-existente (não causado pelo drag, afeta os 5 tipos editáveis igualmente) foi documentado, não corrigido: republicar uma EDIÇÃO de conteúdo já publicado falha com "já existe" — só a criação/primeiro-publish foi afetada pela integração do drag, e essa continua funcionando perfeitamente. Detalhes completos em `docs/CHECKPOINT_ETAPA11_IMPORTACAO_EXPORTACAO_BIBLIOTECA_LIVRO.md` (`## 0-10`).
- **Reconfirmação independente (rodada seguinte, sem alterar código)**: as mesmas duas lacunas foram reauditadas do zero — releitura das fontes sem citar o checkpoint anterior, leitura direta do código (`CamposCapituloSection.tsx`/`bookDragServerActions.ts`/`page.tsx` da Biblioteca real) para confirmar a Questão 1, e um gesto de browser real reexecutado do zero (não reaproveitando nenhuma evidência anterior) para a Questão 2, incluindo os 6 casos inválidos. Harnesses recompilados e reexecutados (19/19 + 6/6), `tsc`/`build` limpos, fixtures novas criadas e removidas ao final contra o Supabase real. Nenhuma divergência encontrada em relação ao que a rodada anterior já tinha registrado — confirma que aquele registro refletia trabalho real, não uma narrativa não verificada. Detalhes em `docs/CHECKPOINT_ETAPA11_IMPORTACAO_EXPORTACAO_BIBLIOTECA_LIVRO.md` (`## 0-11`).

### Etapa 12 — Conteúdo de mesa e homebrew — **Etapa 12 concluída — validação integrada aprovada**
- **Correção 1 (migration 0026)**: a leitura de `campaign_content_documents` era protegida só por `campaign_id` + policy `anon, authenticated` — nunca autorização real. Corrigida: `campaign_members` (só o dono via backfill), `is_campaign_member`/`can_manage_campaign_content`/`can_read_campaign_content`, RLS revisada.
- **Correção 2 (migration 0027, esta sessão)**: fecha o motivo pelo qual só o dono podia ser membro real — não havia NENHUM caminho para um jogador virar membro. `/join/[token]` agora exige sessão Supabase Auth (reaproveitando `LoginForm`/`signInWithPassword`/`signUpDevNarrator`, mesma infra do narrador) e chama `accept_campaign_invite` (RPC transacional, idempotente por usuário) para criar/ativar `campaign_members` com `role='player'`. Também adiciona `campaign_content_references` (índice estruturado de dependências, recalculado a cada publicação na MESMA transação) e reescreve o diagnóstico de impacto para consultá-lo como fonte primária (a varredura por substring em personagens vira sinal secundário).
- **Por que o status não desce ainda**: referências continuam PARCIAIS (cobrem requisitos + condição em efeitos + propriedades de item, herdado de `coletarReferenciasBrutas` da Etapa 11 — não cobrem item concedido, runa referenciada explicitamente, efeitos compostos, companheiro/Trama); e absolutamente nada do fluxo de autenticação/membership/RLS/índice foi verificado contra um Supabase real nesta sessão.
- **Correção 3 (migration 0028, esta sessão)**: `campaign_members` prova pertencimento à campanha, mas nunca provou qual `campaign_profile`/`character` é do jogador. Adiciona `campaign_profiles.user_id` (nullable, preferência explícita do pedido em vez de tabela associativa nova) + RPCs transacionais `claim_campaign_profile`/`create_and_claim_campaign_profile` (nunca inferidas por nome ou pelo primeiro perfil livre, corrida de reivindicação dupla fechada por `for update` + índice único parcial) + funções de autorização aditivas (`can_access_campaign_profile`/`can_manage_campaign_profile`/`can_read_character`/`can_manage_character`) + mais uma família de referência indexada (`item_slug` de `conceder_item`/`consumir_item`). UI: `ClaimProfileClient` inserida em `/join/[token]` entre o aceite de convite e o `JoinClient` existente.
- **Limitação real e deliberada**: a RLS de `campaign_profiles`/`characters` continua a mesma policy aberta herdada desde a migration 0013 — as funções de autorização desta correção existem mas nenhuma RLS as usa ainda; flipar isso com segurança exige migrar ~15 funções de `character/storage.ts` para o client autenticado e provar que nenhum fluxo anônimo existente quebra, um refactor de storage maior que esta correção deliberadamente não tentou dentro do escopo seguro da sessão.
- **Correção 4 (migration 0029, esta sessão)**: auditoria completa das ~15 funções de `character/storage.ts` (matriz no checkpoint) — achado central: a real superfície de risco era muito menor que a presumida. A maioria já é autenticada (client "scoped" do narrador) ou explicitamente dev-only; só as 2 funções que o JOGADOR usa de verdade (`get_character_for_profile_session`/`save_character_for_profile_session`) tinham uma lacuna real — já eram `SECURITY DEFINER` protegidas por token de sessão, mas o token nunca teve vínculo com `auth.uid()`. Corrigidas (mesma assinatura, `create or replace`) para também exigir `auth.uid() = campaign_profiles.user_id` quando o perfil foi reivindicado. Auditoria de referências também fechada: "efeitos compostos/filhos" já eram cobertos (a árvore de teste/resistência é achatada em irmãos no MESMO array antes do payload público — não era lacuna, era suposição errada sobre o formato); `modificar_instancia`, instalar/remover/ativar runa como efeito e `efeito_temporario` foram auditados e confirmados SEM campo de referência a outro conteúdo — cobertura de `campaign_content_references` está completa no escopo real do schema.
- **Correção 5 (migration 0030, esta sessão)**: fecha a lacuna que a correção 4 deixou explícita — a RLS de TABELA de `campaign_profiles`/`characters` continuava `anon, authenticated USING(true)`. Auditoria revalidada (não presumida) confirmou que TODAS as funções dev/legado (`createCharacter`/`updateCharacter`/`getCharacter`/`listCharacters`/`deleteCharacter`) usam `getContentClient()` — anon puro, SEMPRE, mesmo com sessão ativa — e que TODA função de produto (narrador em `character/storage.ts`; toda `campaign_profiles` em `table/storage.ts`) já usa `getScopedTableClient()`. Isso tornou seguro remover `authenticated` das policies antigas (narrowed para `anon`, dev/legado 100% preservado) e substituí-lo por 4 policies novas por tabela usando os helpers de autorização (owner administra tudo da campanha; jogador só lê/edita seu próprio perfil/personagem, com membership `active` confirmada). Dois helpers da correção 3 foram corrigidos no processo: aceitavam `profile.user_id = auth.uid()` sem checar que a membership ainda estava `active` — um jogador `removed` continuaria passando; corrigido.
- **Desvio da correção 5 (REJEITADO E SUBSTITUÍDO na correção 6, ver abaixo)**: SELECT de `campaign_profiles` para `authenticated` permitia qualquer membro ativo ler todos os perfis da própria campanha — justificado pela tela de reivindicação (`ClaimProfileClient`). A correção 5 também preservou a policy `anon` intacta (só narrowed, não removida) em `campaign_profiles`/`characters`, justificada por 5 funções "dev/legado".
- **Correção 6 (migration 0031, esta sessão)**: reabre e rejeita as duas justificativas da correção 5. Auditoria função-a-função (não por nome/comentário do arquivo, ver checkpoint §0-6.1) encontrou que `updateCharacter` (usada por `MesaDetailClient.tsx`, `table/endRound.ts`, `table/endScene.ts` — dashboard REAL do narrador) e `listCharactersForCampaign` (usada por `/join/[token]/page.tsx`, sempre pós-login e pós-aceite de convite) NÃO eram dev-only como presumido — eram produto real rodando com `getContentClient()` (anon puro). Migradas, junto com as 4 funções genuinamente dev-only, para `getScopedTableClient()`. Com isso, a policy `anon` foi REMOVIDA por completo (`drop policy`, não narrow) de `campaign_profiles`/`characters`, com `revoke all ... from anon` explícito. O SELECT de `campaign_profiles` para `authenticated` foi reescrito para "owner vê tudo; jogador lê SOMENTE o próprio perfil" — a RPC mínima `list_claimable_campaign_profiles` (`SECURITY DEFINER`, exige `auth.uid()` + membership ativa, devolve só id+nickname dos perfis não reivindicados) substitui a dependência do SELECT amplo em `ClaimProfileClient`/`/join/[token]/page.tsx`. Consequência aceita, precisada após reauditoria (checkpoint §0-6.8): os scripts de teste (`scripts/test-character-storage.ts` e afins) rodam fora de qualquer contexto de request HTTP, então nunca conseguem carregar uma sessão real — ficaram estruturalmente incompatíveis com as duas tabelas, não só "sem login agora". As rotas dev (`/dev/character-sheet`, `/dev/table`, `/dev/join/[campaignId]`) continuam alcançáveis sem login, mas leem listas vazias/falham a escrita sem uma sessão real; com uma sessão real (visitante logado), voltam a funcionar plenamente. Nenhum dos dois casos fazia parte da verificação automatizada.
- **Referências**: não reabertas nesta correção (nem na 5) — nenhuma lacuna concreta nova foi encontrada; cobertura permanece "completa no escopo representável pelo schema público e pelos consumidores atuais".
- **Correção do status (auditoria estática pós-correção 6, sem alteração de código)**: encontradas 2 falhas críticas não fechadas pela correção 6 — `profile_sessions` continuava com CRUD aberto a `anon`/`authenticated` (migration 0009, nunca tocada); `campaign_profiles_authenticated_update` permitia ao jogador gravar `active_character_id` apontando para personagem alheio (sem restrição por coluna). Status baixado para "Implementação parcial — correções críticas de autorização de sessões e vínculo de personagem pendentes" até a correção 7 fechar as duas.
- **Correção 7 (migration 0032, esta sessão)**: fecha as duas falhas críticas, mais uma TERCEIRA encontrada na reauditoria obrigatória desta correção: `campaign_profiles_dev_transition_*` (nome real pós-rename da migration 0007) nunca foi removida por nenhuma correção anterior — as migrations 0030/0031 erraram o nome (`campaign_profiles_dev_anon_*`, que já não existia desde 0007) e por isso nunca a dropavam de fato; `campaign_profiles` continuou com CRUD completo aberto a `anon` até agora, apesar de toda a narrativa das correções 5/6. Migration 0032: dropa as policies `*_dev_transition_*` de `campaign_profiles`/`profile_sessions` pelos nomes corretos; revoga `anon` de ambas; reescreve `campaign_profiles_authenticated_update` para owner-only (jogador perde UPDATE direto por completo); cria 7 RPCs orientadas a uma operação concreta (`enter_campaign_profile`, `heartbeat_profile_session`, `leave_campaign_profile`, `validate_profile_session_token`, `expire_stale_profile_sessions`, `force_release_campaign_profile`, `set_campaign_profile_active_character`); estabelece `characters.profile_id` como vínculo canônico de propriedade de personagem (substituindo a formulação antiga de que `active_character_id` seria autoridade suficiente sozinho); reforça `get/save_character_for_profile_session` para exigir `characters.profile_id = p_profile_id` também. `table/storage.ts` reescrito para chamar as RPCs em vez de acessar as tabelas diretamente, mantendo as mesmas assinaturas exportadas (nenhum caller precisou mudar).
- **Validação integrada contra Supabase real (migrations 0033/0034, esta sessão)**: aplicadas as 8 migrations pendentes da Etapa 12 (0025-0032 — nenhuma tinha sido aplicada antes neste projeto, confirmado por `list_migrations`/`list_tables`) e rodada a matriz completa de RLS/RPC com identidades reais (owner, Player A, Player B, invited, removed, outsider, anon), via `SET ROLE`/`request.jwt.claim.sub` — nunca service role como prova. Os 2 ataques críticos pedidos foram reproduzidos e bloqueados em toda camada, inclusive redirecionamento de personagem contra dado corrompido administrativamente. Durante a validação, encontradas e corrigidas 2 falhas concretas NOVAS: (1) `revoke all on function X from public` nunca bloqueava `anon` neste projeto — uma default privilege de schema concede EXECUTE a `anon` em toda função nova; corrigido com `revoke execute ... from anon` explícito nas 19 RPCs/helpers authenticated-only da Etapa 12 (migration 0033); (2) um membro `removed` com sessão de personagem já emitida continuava lendo/escrevendo o próprio personagem normalmente, porque nenhuma das RPCs de sessão checava `campaign_members.status`; corrigido exigindo `is_campaign_member` (membership ativa) quando o perfil já foi reivindicado (migration 0034). Fluxo de conteúdo (homebrew → publicação → leitura efetiva) testado sem regressão. Fixtures de teste completamente removidas ao final.
- **Retificação do status anterior (esta sessão, antes de qualquer teste novo)**: a rodada `0-8` só tinha comprovado concorrência SEQUENCIAL (chamadas uma depois da outra via MCP) e não tinha conseguido diagnosticar/executar o aceite de browser — o status "Implementação concluída — aceite de browser pendente" registrado ali confundia essas duas lacunas com uma pendência só de ambiente. Corrigido para "Implementação parcial — validação concorrente e aceite de browser pendentes" antes de iniciar esta rodada, sem regredir para a redação genérica anterior (RLS/RPC seguem genuinamente comprovadas).
- **Concorrência simultânea real + aceite de browser (esta sessão)**: construído `scripts/dev/validate-campaign-session-concurrency.mjs` — clientes `@supabase/supabase-js` independentes, sessões reais (`signInWithPassword`), disparando via `Promise.allSettled` (nunca `await` sequencial, nunca `SET ROLE` alternado na mesma conexão, service role só para preparar/limpar fixtures). Os 6 cenários pedidos (reivindicação simultânea; dupla entrada simultânea; heartbeat vs. saída; `force_release` vs. heartbeat; save vs. troca de personagem ativo, com variante hostil contra personagem de outro perfil; expirar vs. heartbeat) rodaram — **7/7 aprovados** (invariantes mantidos mesmo quando o vencedor de cada corrida variava entre execuções, evidenciando simultaneidade real). Servidor local diagnosticado (PID/porta/HTTP real, não só a rota raiz) antes do browser: processo anterior corrompido por `npm run build` rodando ao mesmo tempo que o `next dev` já ativo (ambos compartilham `.next`); nenhuma ação destrutiva manual foi necessária (o processo antigo terminou sozinho durante o diagnóstico); servidor novo iniciado via `npm run dev` (o mesmo script de `package.json`) e confirmado saudável por `curl` real. Aceite de browser executado por completo: narrador (login→dashboard→mesa→vínculo de personagem→Encerrar Rodada/Cena→biblioteca→logout), Player A (convite→login→aceite→reivindicação→ficha→edição→salvar→reload→persistência confirmada), Player B (mesmo fluxo + teste hostil de acesso direto à ficha do perfil A, corretamente bloqueado), membro removido (fixture administrativa + reload de sessão previamente válida, corretamente bloqueado, sem loop/erro), e o roster do `/join` (bloco "Perfis (N)", contagem e itens batendo com o escopo restrito real, sem vazamento). **1 bug real encontrado e corrigido**: `revalidatePath` chamado durante o render de `/join/[token]/page.tsx` (via `acceptCampaignInvite`), quebrando o aceite de convite — Next.js proíbe `revalidatePath` fora de Server Actions/Route Handlers reais; removido (redundante mesmo quando funcionava, já que `/mesas/[campaignId]` é `force-dynamic`). Fixtures desta rodada completamente removidas ao final, confirmado por contagem. Detalhes completos em `docs/CHECKPOINT_ETAPA12_CONTEUDO_MESA_HOMEBREW.md` (`## 0-9`).
- **Por que o status sobe para "Etapa 12 concluída"**: concorrência SIMULTÂNEA real comprovada (não só sequencial); servidor local saudável e diagnosticado corretamente; aceite de browser completo para narrador, Player A, Player B (com teste hostil) e membro removido; roster sem regressão; console/rede limpos em todas as telas; nenhuma falha crítica/média não corrigida permanece; RLS/RPC seguem comprovadas contra o Supabase real (`## 0-8`); todas as fixtures (das duas rodadas) removidas.
- Um bug real de bundling foi encontrado e corrigido na correção 2: um `export type` residual em `campaignContentServerActions.ts` ("use server") quebrava o build assim que o módulo passou a ser importado por um Server Component pela primeira vez — removido.
- **Verificado**: TypeScript, build, 18 verificações focadas em Node puro (Etapa 12, inalteradas) + 78 verificações estruturais (`scripts/dev/validate-profile-session-lockdown.mjs`, inalterado nesta rodada) + matriz de RLS/RPC/ataques contra Supabase real + script de concorrência simultânea (7/7) + aceite de browser completo. Detalhes completos em `docs/CHECKPOINT_ETAPA12_CONTEUDO_MESA_HOMEBREW.md` (`## 0-8`, `## 0-9`).
- A Etapa 11 permanecia, no momento desta rodada, "Implementação parcial — integração editorial de drag pendente" — **nota factual**: numa rodada posterior, dedicada exclusivamente à Etapa 11, essa pendência foi fechada; ver `docs/CHECKPOINT_ETAPA11_IMPORTACAO_EXPORTACAO_BIBLIOTECA_LIVRO.md` (`## 0-9`) e a seção da Etapa 11 acima neste mesmo plano. Nada nesta correção da Etapa 12 foi alterado por essa rodada posterior.

---

## 2.1 Consolidação final do Editor Universal (rodada final, 25/07/2026)

Rodada final acelerada, dividida em 3 fases condicionais: **Fase A** — corrigir a divergência operacional da Etapa 8 (reaplicação/pilhas configuradas no editor sem efeito real no executor); **Fase B** — fechar o único residual real de Etapa 5 (`expectedVersion` concorrente, nunca testado com 2 sessões reais); **Fase C** (esta seção) — condicional à aprovação total de A e B, consolidação documental/factual de todo o Editor Universal.

### Matriz Etapas 0–12

| # | Etapa | Status | Checkpoint | Evidência de implementação | Evidência de browser/DB | Harness | Pendência/bloqueio |
|---|---|---|---|---|---|---|---|
| 0 | Auditoria inicial | Concluída | `AUDITORIA_EDITOR_UNIVERSAL_CONTEUDO.md` | Auditoria estática completa do estado pré-editor | N/A (auditoria, não implementação) | N/A | Nenhuma |
| 1 | Camada canônica de definições e validação | Concluída | `CHECKPOINT_ETAPA1_SCHEMA_CANONICO.md` | Schema canônico, catálogo de efeitos, preservação de campos desconhecidos | Coberta indiretamente pelas etapas seguintes (fundação, sem UI própria) | Harnesses das etapas seguintes exercitam esta camada | Nenhuma |
| 2 | Lista administrativa e inspeção | Concluída | `CHECKPOINT_ETAPA2_ADMIN_BIBLIOTECA_READONLY.md` | `is_content_admin()`, `getContentAdminStatus`, lista/diagnóstico read-only | Confirmada pelo uso contínuo do gate admin em todas as rodadas de browser subsequentes | N/A (read-only) | Nenhuma |
| 3 | Editor universal de campos básicos | Concluída | `CHECKPOINT_ETAPA3_EDITOR_CAMPOS_BASICOS.md` | `content_drafts`, optimistic locking, 5 content types editáveis | Confirmada indiretamente — todo fluxo de criação/edição das rodadas posteriores passa por este editor | N/A | Nenhuma |
| 4 | Construtor de efeitos MVP | Etapa 4 concluída — aceite operacional e de browser aprovado | `CHECKPOINT_ETAPA4_CONSTRUTOR_EFEITOS_MVP.md` §9.3 | 6 tipos de efeito originais, modos de automação derivados | Consumo operacional real de `aplicar_condicao` confirmado (personagem real, persistência, bloqueio de duplicata) | Harness próprio, inalterado | Nenhuma |
| 5 | Publicação, versões e changelog | **Etapa 5 concluída — publicação, versões, changelog e concorrência aprovados** | `CHECKPOINT_ETAPA5_PUBLICACAO_VERSIONAMENTO.md` | RPCs transacionais (`publish_content_draft`/`archive_content_document`), diff/histórico/impacto | Ciclo completo criar/editar/salvar/revisar/republicar/arquivar + importação `atualizacao` end-to-end + **2 sessões reais provando `expectedVersion`** (rodada final) | N/A (fluxo transacional, coberto por SQL direto + browser) | Nenhuma |
| 6 | Adaptadores e edição de legados | Etapa 6 concluída — adaptadores e edição de legados aprovados | `CHECKPOINT_ETAPA6_ADAPTADORES_LEGADO.md` | 3 adapters (item/spell/talent), classificação por campo/efeito, validação de perda | Ciclo salvar→publicar→republicar sobre conteúdo genuinamente legado confirmado | N/A | Nenhuma |
| 7 | Teste, resistência e efeitos compostos | Etapa 7 concluída — testes, resistências e efeitos compostos aprovados | `CHECKPOINT_ETAPA7_EFEITOS_COMPOSTOS.md` | `teste_resistencia`, `modificar_margem`, `alterar_dano_recebido` | Reedição de árvore composta confirmada sem duplicação (bug real corrigido e provado) | `validate-composite-effects.mjs` 15/15 | Consumo operacional (resolvedor sem gatilho de UI) — decisão de escopo, não falha |
| 8 | Efeitos temporários, cadências, usos e consumos | **Etapa 8 concluída — efeitos temporários, cadências, reaplicação e consumos aprovados** | `CHECKPOINT_ETAPA8_TEMPORARIOS_CADENCIAS_CONSUMOS.md` | `efeito_temporario`, `acao_reacao_adicional`, uso/cadência genérico | **Fase A desta rodada**: reaplicação/pilhas corrigidas end-to-end (serializador+executor+UI); 3 das 4 políticas (`substituir`/`acumular_pilha`/`ignorar`) provadas ao vivo com personagem real, DB real, cadência de rodada real até expiração | `validate-temporary-effects.mjs` 25/25 (8 casos novos, regressão provada) | Política `manual` não testada ao vivo (trivial, código inalterado) — limitação factual, não bloqueante |
| 9 | Inventário, equipamentos, runas e mercado | Etapa 9 concluída — inventário, runas e mercado aprovados | `CHECKPOINT_ETAPA9_INVENTARIO_RUNAS_MERCADO.md` | `rune` como 4º content type, defaults de equipamento, `modificar_instancia` | MIT reparado via executor real, runa instalada em instância real, compatibilidade de slot honrada | `validate-inventory-runes-market.mjs` 22/22 | Concessão/consumo/disponibilidade continuam lembrete (decisão de escopo) |
| 10 | Drones, robôs, companheiros e Trama | Etapa 10 concluída — companheiros, drones, robôs e Trama aprovados | `CHECKPOINT_ETAPA10_COMPANHEIROS_TRAMA.md` | 6 tipos novos (`companheiro`, `modificar_companheiro`, `acao_companheiro`, `programar_gatilho`, `parear`, `acao_trama`) | Reedição sem duplicação/ressurreição de efeitos removidos (bug real corrigido e provado) | `validate-companions-trama.mjs` 24/24 | Nenhuma |
| 11 | Importação, exportação e Biblioteca do Livro | Etapa 11 concluída — integração editorial de drag aprovada | `CHECKPOINT_ETAPA11_IMPORTACAO_EXPORTACAO_BIBLIOTECA_LIVRO.md` | Pacote `ruptura-content-package` v1, hash canônico, drag nativo HTML5 | Drag real (eventos DOM reais, não chamada direta), round-trip export/import sem perda, reconfirmação independente | `validate-import-export-book.mjs` 19/19 + `validate-editorial-drag.mjs` 6/6 | Nenhuma |
| 12 | Conteúdo de mesa e homebrew | Etapa 12 concluída — validação integrada aprovada | `CHECKPOINT_ETAPA12_CONTEUDO_MESA_HOMEBREW.md` | RLS/RPC por membership real, `campaign_content_references`, sessão de personagem vinculada a `auth.uid()` | Concorrência simultânea real (7/7) + aceite de browser completo (narrador/Player A/Player B/removido) contra Supabase real | 18 + 78 verificações estruturais | Referências cobrem só o escopo representável pelo schema atual (documentado, não bloqueante) |

### Critérios do aditivo mapeados a evidência concreta

| Critério (aditivo) | Atendido? | Evidência |
|---|---|---|
| Conteúdo localizável no admin | Sim | Etapa 2 (lista/busca/filtro), usado em toda rodada de browser desde então |
| Tipos comuns criáveis sem código | Sim | Etapas 3/6/9 — 5 content types (magia/item/runa/talento/capítulo) via UI, nenhum novo tipo exige deploy de código |
| Automações comuns montáveis sem JSON | Sim | Etapa 4/7/8/9/10 — Construtor Universal de Efeitos, 22 tipos, sem edição de JSON bruto no fluxo principal |
| Grau de automação declarado, nunca mentiroso | Sim | `modo_automacao` (Automático/Assistido/Lembrete/Narrativo rastreado) sempre derivado do catálogo real, nunca escolhido livremente; auditado tipo a tipo em cada etapa |
| Rascunho | Sim | Etapa 3 — `content_drafts`, isolado do publicado, optimistic locking |
| Validação | Sim | Validação em 2 camadas: genérica no salvamento (`draftValidation.ts`), específica de schema/publicação (`validarEfeitoParaPublicacao` e afins) — bloqueante de verdade, provado em toda etapa |
| Publicação | Sim | Etapa 5 — RPC transacional, versão computada no SQL, hash determinístico |
| Arquivamento | Sim | Etapa 5 — `status='archived'`, some do jogo por construção, nunca apaga dado |
| Changelog | Sim | Etapa 5 — `content_changelog` estendido (versões, autor, resumo, caminhos, impacto), confirmado em toda republicação desta rodada |
| Versão | Sim | Semver `a.b.c`, `+patch` automático, autoridade única no SQL |
| Legado preservado | Sim | Etapa 6 — 3 adapters, campos desconhecidos preservados byte a byte, conversão nunca automática sem confirmação |
| Referências seguras | Sim (no escopo do schema atual) | Etapa 11/12 — `content_dependencies`/`campaign_content_references`, nunca confia em slug do client sem validar existência |
| Separação modelo × instância | Sim | Documentado e reforçado em cada etapa (ex.: Etapa 8 — payload publicado nunca tem `stacks`/`remainingRounds`; Etapa 9 — nunca tem `mitAtual`/`cargasAtual` — confirmado por harness dedicado) |
| Consumo pelo VTT (ficha real) | Sim | Fase A desta rodada — item publicado usado de verdade via `/dev/character-sheet`, efeito real criado/reaplicado/expirado num personagem real |
| Evolução via efeitos reutilizáveis | Sim | Catálogo único de 22 tipos reaproveitado por magia/item/talento/runa, nunca um efeito exclusivo de um content type sem justificativa de schema |
| Fluxo completo de criação de magia, do início ao fim | Sim | Exercitado repetidamente nas rodadas de aceite de browser das Etapas 4–10 (criar → configurar efeito → salvar → revisar → publicar → consumir) |
| Documentação de limitações reais | Sim | Cada checkpoint tem seção de limitações explícitas (ex.: `acao_reacao_adicional` sempre lembrete; concessão/consumo de item sem executor; política `manual` de reaplicação não testada ao vivo) — nunca omitidas |

### Itens explicitamente fora de escopo (nunca bloqueantes)

Biblioteca do Livro completa (modo leitura/resumo), drag Livro→ficha (operacional, distinto do drag editorial já implementado), importador completo do Notion, e qualquer outra feature futura citada no aditivo como pós-MVP — nenhum destes item foi tratado como pendência desta consolidação.

### Verificação técnica final desta rodada

`npx tsc --noEmit` sem erros; `npm run build` sucesso; `next-env.d.ts` revertido (auto-tocado duas vezes pelo dev server ao longo da rodada, revertido antes de cada commit). Todos os harnesses tocados por Fase A reexecutados sem regressão (`validate-temporary-effects.mjs` 25/25, `validate-inventory-runes-market.mjs` 22/22, `validate-companions-trama.mjs` 24/24, `validate-post-mvp-effects.mjs` 17/17). Nenhum harness de Fase B foi tocado (nenhum código de publicação alterado). Nenhum bug crítico/médio pendente. Todas as fixtures desta rodada (`zz_e2e_editor_final_*`, `zz_e2e_editor_finalb_*`) removidas, confirmado por contagem zero em cada tabela tocada.

### Status final

**"Editor Universal concluído — critérios formais e técnicos atendidos."**

Todas as 13 etapas (0–12) têm evidência de implementação e de browser/DB real; todos os critérios do aditivo mapeados acima têm evidência concreta; nenhum bug crítico ou médio permanece sem correção ou sem documentação explícita como limitação de escopo; TypeScript/build/harnesses passam; fixtures limpas. As poucas lacunas restantes (política `manual` de reaplicação não testada ao vivo; referências limitadas ao escopo do schema atual; consumo operacional de alguns efeitos permanecendo "lembrete" por decisão de produto) são limitações reais e documentadas, não pendências ocultas — nenhuma delas contradiz os critérios formais do aditivo, que trata "modo de automação corretamente declarado" como suficiente, nunca exigindo automação total de todo efeito possível.

---

## 3. Migrations prováveis (NÃO aplicadas nesta etapa)

Listadas por probabilidade e ordem de necessidade real, com justificativa vinda da auditoria. Nenhuma delas deve ser criada ou aplicada até a etapa correspondente ser autorizada.

1. **Coluna(s) de rascunho/publicação em `content_documents`** — hoje `status` já existe mas só tem o valor `'published'` em uso; provavelmente só precisa de uma constraint/enum (`draft`/`published`/`archived`) e talvez `published_at`, `published_by`. Baixo risco — coluna já existe, é extensão de domínio de valor.
2. **Tabela/coluna de autorização admin** — algo como `admin_users` (ou uma claim/role em `profiles`, se existir tabela de perfil — verificar na Etapa 2) para suportar RLS de escrita em `content_documents`/`content_packs`. Sem isso, nenhuma policy de INSERT/UPDATE pode ser escrita com segurança.
3. **Policies de escrita em `content_documents`/`content_packs`** restritas ao novo papel de admin — hoje simplesmente não existem (só leitura pública + service role). Precisa ser cuidadosamente escrita para não abrir escrita a `anon`/`authenticated` genérico.
4. **Colunas para versionamento incremental** (`content_documents.editor_version` ou similar, distinto do `version`/`versao` do payload) — se a decisão de produto for versionar automaticamente a cada publicação pelo editor (Etapa 5).
5. **Possível nova tabela `content_drafts`** (alternativa à opção 1) — se a decisão de produto for **não** misturar rascunho e publicado na mesma linha de `content_documents` (evita rascunho aparecer em querys existentes por engano, já que todas as querys hoje filtram `status='published'` e um rascunho com outro status já ficaria automaticamente invisível — então a opção 1 provavelmente é suficiente e mais simples).
6. **Decomposição de singletons em coleções** (`character_rule` → tabelas/rows próprias de perícia/atributo/vertente, se a decisão do item em aberto §7.1 do schema canônico for por decompor) — a mais arriscada das seis, pois muda a forma como `getCharacterRules()` e consumidores atuais leem esses dados; exigiria também atualizar `src/lib/content/queries.ts` e qualquer consumidor de `character_rule` no app. Só fazer se houver necessidade real de editar perícia/atributo isoladamente — adiar até o produto pedir.
7. **Correção do drift item 119→120** no manifesto (`content/ruptura_core_manifest_v0_1.json`) — não é migration de banco, é atualização do arquivo fonte; mencionado aqui porque a Etapa 2 (diagnóstico) provavelmente vai expor esse caso e alguém vai perguntar "por que isso não foi corrigido" — resposta: fora do escopo desta auditoria (regra "não modificar conteúdos publicados / não reescrever payloads existentes").

Nenhuma migration acima foi criada nem aplicada. `list_migrations`/estado do banco não foi verificado via MCP nesta etapa por não ser necessário para a auditoria estática do repositório — se necessário confirmar o estado remoto do Supabase antes da Etapa 2, isso deve ser uma ação explícita e autorizada à parte.

---

## 4. Riscos

| Risco | Origem do achado | Mitigação proposta |
|---|---|---|
| Editor promete automação que o motor não entrega (ex.: publicar magia com `aplicar_condicao` parecendo "automática") | Auditoria §4.1 — `aplicar_condicao` é o efeito mais comum sem executor | `modo_automacao` derivado do catálogo de efeitos, nunca escolhido livremente (schema canônico §4.2) |
| Divergência silenciosa entre manifesto e dados (já aconteceu: item 119→120) | Auditoria §2 | Diagnóstico técnico da Etapa 2 deve comparar manifesto × contagem real e alertar, não silenciar |
| Falta de admin role bloqueia toda escrita seguramente | Auditoria §9 | Tratar como pré-requisito bloqueante da Etapa 2, não deixar para depois |
| Efeitos de talento bespoke (`talentEngine.ts`) tentados a entrar no catálogo genérico prematuramente | Auditoria §4 | Seguir regra do aditivo §20 — só generalizar quando 2+ conteúdos precisarem do mesmo efeito |
| `estatisticas` de item sem contrato pode gerar formulário instável por categoria | Auditoria §5.6 | Tratar como somente leitura até a Etapa 3 decidir sub-schema por categoria |
| Runas com `additionalProperties: true` podem esconder dados que o editor não preserva corretamente | Auditoria §3 | Preservação de campos desconhecidos (schema canônico §6) cobre isso desde a Etapa 1 |
| Decompor singletons (`character_rule`, `master_table`) pode quebrar consumidores existentes (`getCharacterRules()` etc.) | Schema canônico §7 | Adiar decomposição até haver necessidade de produto comprovada; não incluir no MVP |

---

## 5. Divisão das próximas etapas — resumo de decisão

Nenhuma etapa além da 0 está autorizada por este checkpoint. A ordem recomendada de execução, uma vez autorizada etapa a etapa, é a já definida no aditivo (1 → 12), com os ajustes de esforço anotados acima. O próximo passo, quando solicitado, é a **Etapa 1 — Camada canônica de definições e validação**, cujo primeiro artefato concreto deveria ser o catálogo de efeitos (não o formulário de UI), pois é a peça que mais acumula risco de retrabalho se adiada.
