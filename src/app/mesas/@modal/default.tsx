/**
 * Fallback do slot `@modal` — nenhum conteúdo em toda navegação normal
 * dentro de /mesas (só a rota interceptada `(...)ficha` preenche este
 * slot).
 */
export default function ModalDefault() {
  return null;
}
